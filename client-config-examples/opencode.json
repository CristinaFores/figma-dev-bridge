{
  "//": "OpenCode config lives at ~/.config/opencode/opencode.jsonc. Uses 'type: local' and 'command' as an array.",
  "mcp": {
    "figma-dev-bridge": {
      "type": "local",
      "command": ["npx", "-y", "figma-dev-bridge"],
      "enabled": true
    }
  }
}
