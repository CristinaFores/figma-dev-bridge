{
  "//": "Generic stdio MCP config. Adjust the key format to match your client.",
  "mcpServers": {
    "figma-dev-bridge": {
      "command": "npx",
      "args": ["-y", "figma-dev-bridge"],
      "type": "stdio"
    }
  }
}
